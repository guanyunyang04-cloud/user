# T0 Project 治理层

快照日期：`2026-04-12`

## 1. 目标
- 防止盘中实验分脑越级影响正式生产主线。
- 保持 `paper / live / RL / strategy` 的边界清晰。

## 2. 执行前四检
- 这项工作是否仍属于实验分脑范围。
- 是否会误伤或误改 `daily_research` 正式结论。
- 是否跳过了 `paper -> 人工确认 -> 真实接口` 顺序。
- 当前依赖和上下文是否足以支撑接入。

## 3. 偏移检测
- 目标偏移：
  - 把实验目标误变成生产目标。
- 上下文偏移：
  - 把保护性骨架当成已打通能力。
- 行为偏移：
  - 还没补验证链，就先谈全自动。

## 4. 修复机制
- 轻度偏移：
  - 刷新交接包和当前边界。
- 中度偏移：
  - 回退到 `paper` 模式，补验证链。
- 重度偏移：
  - 冻结真实接口讨论，只允许继续做抽象层和证据链。
