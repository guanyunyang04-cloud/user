# T0 Project Working Memory

## 1. 当前优先级
- 保持 `t0_project` 与 `daily_research` 正式执行主线隔离
- 继续把盘中实验定位为研究原型，而不是可直接实盘部署系统
- 如果推进真实执行能力，优先补齐执行抽象层的可验证边界

## 2. 当前安全边界
- `live` 模式仍是保护性骨架，不应视为已打通自动下单
- 新实验结果不得静默替换 `daily_research` 的默认执行主线
- 真实适配器补齐前，应优先使用 `paper` 模式
