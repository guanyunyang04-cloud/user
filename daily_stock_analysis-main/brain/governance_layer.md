# Daily Stock Analysis 治理层

快照日期：`2026-04-12`

## 1. 目标
- 防止多入口产品仓库在接管时边界失焦。
- 保持 brain、body_map 和仓库原生 AI 资产的长期一致。

## 2. 执行前四检
- 当前任务属于哪个入口和模块边界。
- 当前动作是否会误影响其它入口。
- 相关验证矩阵是否清楚。
- 是否需要同步 `AGENTS.md / CLAUDE.md` 或其它协作资产。

## 3. 偏移检测
- 目标偏移：
  - 把独立产品任务混进 `daily_research` 主线。
- 上下文偏移：
  - 没判边界就盲扫或盲改。
- 行为偏移：
  - 只改实现，不同步 brain 和 AI 资产。

## 4. 修复机制
- 轻度偏移：
  - 回到交接包，重新判定入口和边界。
- 中度偏移：
  - 回退到单一入口视角，分解任务后再继续。
- 重度偏移：
  - 冻结跨入口改动，只允许先补边界说明和验证链。
