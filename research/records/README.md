# Research records

This directory is the stable, compact evidence surface. A completed or retired
experiment keeps only its final report, key metrics, semantic contract, and any
checkpoint registered in `research/models/registry.json`.

Raw predictions, per-job logs, temporary packs, failed runs, and append-only
report history are rebuildable process material and do not belong here.

The current index is `seq100/index.json`.
