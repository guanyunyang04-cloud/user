# 项目评估与维护结论

更新时间：`2026-03-25`

## 1. 评估范围
本轮评估覆盖整个工作区：

- `daily_research/`
  - 正式维护的日线研究、执行端与 `deep_alpha` 研究主线。
- `t0_project/`
  - 通达信盘中 T+0、执行抽象与强化学习实验区。

本轮评估方法包括：

1. 对齐根目录说明、`daily_research` 文档分工、研究日志与 Git 提交历史；
2. 运行文档守卫、工作区体检与 Python 语法编译检查；
3. 检查项目入口、治理工具与边界文档是否形成闭环。

## 2. 立项以来的阶段演进
从当前文档、研究日志与提交历史看，项目大致经历了六个阶段：

1. `2026-03-14 ~ 2026-03-18`
   - 建立 `baseline`，形成早期 `score + 5d`、市场状态过滤与状态内 profile 研究框架。
2. `2026-03-18 ~ 2026-03-20`
   - 引入 `advanced_ml`，并把执行口径统一为“盘后信号 -> 次日开盘执行”。
3. `2026-03-20 ~ 2026-03-22`
   - `deep_alpha` 转入正式研究框架，开始按历史滚动股票池、`next_open` 与多窗口 walk-forward 评估。
4. `2026-03-22`
   - 项目开始系统化治理，出现 README 分工、归档策略、文档守卫、工作区体检等基础设施。
5. `2026-03-23 ~ 2026-03-24`
   - 执行主线正式冻结为 `advanced_ml (ma50 baseline, lgbm) + liquid500 + next_open`，并把“日频目标更新”固化为默认理解。
6. `2026-03-25`
   - 研究侧继续推进坏市场专项与 `deep_alpha` 最小充分对照；
   - 治理侧开始把“项目评估、热区风险提示、实验分支边界文档”也纳入正式维护范围。

## 3. 当前项目判断
当前项目不是“代码已经乱到无法维护”，而是进入了一个更典型的中后期阶段：

- 主线已经收敛。
  - `daily_research` 的正式执行默认值已明确，`deep_alpha` 的主研究方向也已明确。
- 文档职责基本成型。
  - 根目录 README 负责总览，`daily_research/README.md` 负责当前主线与入口，`daily_research/research_log.md` 负责完整时间线。
- 代码语法层面健康。
  - 本轮 `python -m compileall daily_research t0_project` 通过。
- 真正的维护压力转移到了“热区治理”和“实验边界管理”。
  - 也就是缓存、输出、归档覆盖、实验分支入口清晰度，而不是明显的语法错误。

## 4. 本轮巡检快照
本轮首次通过 `workspace_maintenance.py report` 观察到的关键状态如下：

- Python 源文件：`91`
- `daily_research` 总体积：`347.46 MB`
- `daily_research/cache`：`342.79 MB`
- `daily_research/execution/models`：`2.70 MB`
- `daily_research/execution/output`：`15.44 KB`
- `t0_project`：`979.23 KB`
- `__pycache__` 目录：`4`
- `*.pyc` 文件：`44`
- 当前归档 dry-run 候选：待复核

这组数据意味着：

1. 当前工作区的主要生成物压力集中在 `daily_research/cache`，其余热区相对可控。
2. 工作区虽然已经远小于之前的热区状态，但缓存、执行模型和执行输出依然在持续累积。
3. 项目治理已经从“有没有规则”进入“规则是否足以覆盖真实日常使用”的阶段。

## 5. 当前最重要的风险
### 风险 A：体检工具此前只报体积，不主动解释风险
原有 `workspace_maintenance.py report` 可以输出大小，但无法回答：

- 哪些目录已经越过治理阈值；
- 当前归档策略是否覆盖到了热区；
- 为什么明明有热区，归档候选却仍可能为 `0`。

这会让项目进入“看得见大小、看不见风险”的状态。

### 风险 B：`t0_project` 缺少顶层入口说明
`t0_project` 原先主要依赖长篇背景稿与局部 README，缺少一个顶层“现在怎么理解、哪些入口可用、哪些能力不能误判为可实盘”的说明文件。

这容易带来两类误读：

- 误把实验能力当成正式执行能力；
- 误把背景研究长文当成日常操作入口。

### 风险 C：文档治理对关键文件的覆盖还不完整
治理工具原先重点覆盖了主 README、计划与研究日志，但没有把 `runtime_environment.md` 与 `t0_project` 顶层说明纳入同一套文档守卫。

这会导致项目后续再次出现“主干文档被管住了，边缘关键文档又慢慢漂移”的情况。

## 6. 本轮已完成的维护动作
### 6.1 强化工作区体检
已更新 `daily_research/tools/workspace_maintenance.py`：

- `report` 现在除了打印目录大小，还会打印最新修改时间；
- 会同时汇总默认归档策略的覆盖情况；
- 会对超阈值热区直接给出告警；
- 会在“热区存在但归档 dry-run 为 0”时主动提示治理缺口；
- `--json-out` 与归档 manifest 输出路径现在统一按工作区相对路径解析。

### 6.2 扩大文档守卫覆盖范围
已更新 `daily_research/tools/doc_guard.py`：

- 新增检查 `daily_research/runtime_environment.md`；
- 新增检查 `t0_project/README.md`；
- 为两者补上行数与结构约束。

### 6.3 补齐 `t0_project` 顶层 README
已新增 `t0_project/README.md`，明确：

- 该目录是盘中 T+0 / RL 实验区；
- 它与 `daily_research` 的正式执行主线隔离；
- `execution/` 当前仍是安全边界明确的执行抽象层，而不是已打通的自动实盘系统；
- 哪些目录是源码，哪些目录是生成物。

### 6.4 根目录入口已对齐
已更新根目录 `README.md`，把：

- 本轮项目评估文档；
- `t0_project` 顶层 README；

一起纳入统一入口说明。

## 7. 当前优先级判断
### 优先级 A：完成 `deep_alpha` 正式判决
这是当前最值得继续投入的研究主线，优先级仍高于横向扩更多旁支实验。

### 优先级 B：维持执行主线稳态
默认执行口径已经明确，后续应优先保持其可复核、可回滚，而不是再次让局部高收益分支静默替换默认值。

### 优先级 C：持续校准归档/热区治理
下一轮治理不应再停留在“有归档规则”，而应转向：

- 抽样核对最近一批缓存与输出的实际用途；
- 判断现有 `keep_recent_count` / `keep_recent_days` 是否过宽；
- 必要时补充更细的归档规则或人工审查规则。

### 优先级 D：继续保持 `t0_project` 的安全边界
`t0_project` 可以继续做盘中实验，但不应和 `daily_research` 的正式执行链路混用，更不能把 `live` 适配器骨架误读成已准备好自动实盘。

## 8. 当前不建议做的事
- 不建议直接粗暴清空 `daily_research/cache`，因为其中仍包含活跃研究缓存。
- 不建议把 `t0_project` 的实验结论直接推广为 `daily_research` 默认值。
- 不建议重新把 README、计划、日志混成一份时间流水账。
- 不建议在 `deep_alpha` 尚未完成正式判决前，再横向扩更多高成本研究旁支。

## 9. 一句话结论
这个项目的主线、边界和研究方向已经基本收敛；当前最需要修复的，不是基础代码是否“能跑”，而是工作区体检不够解释型、实验分支边界说明不够清晰，以及关键治理文档覆盖不完整这几类中后期维护问题。本轮维护已经把这些问题显式化，并把其中最容易反复失控的部分固化成了工具和文档。
